<div class="container">
  <div class="row">
    <% @deals.each do |deal| %>
    <%= link_to deal_path(deal) do %>
    <div class="col-xs-12 col-md-6 col-lg-6">
      <div class="deal-card">
        <div class="deal-card-photo text-center">
          <%= cl_image_tag deal.photo, height: 300, width: 400, crop: :fill %>
        </div>
        <div class="deal-card-price"><%= deal.price %>€</div>
        <div class="deal-card-description">
          <h2><%= deal.name %></h2>
          <p><%= deal.location %></p>
        </div>
          <img class="deal-card-user" src="https://kitt.lewagon.com/placeholder/users/tgenaitay">
          <a class="deal-card-link" href="#" ></a>
      </div>
    </div>
      <% end %>
    <% end %>
  </div>
</div>
