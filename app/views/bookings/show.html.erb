<div class="newdeal-banner" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url('<%=image_path "banner.jpg" %>');">
  <div class="newdeal-content text-center">
    <h3>Confirmation de la réservation</h3>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-9 col-sm-offset-3">
      <h4>Etes vous sure de vouloir réserver la disponibilité suivante :</h4>
      <p><%= @booking.deal.name %></p>
      <p>Du <%= @booking.start_date.strftime("%A %d %B %Y") %> au <%= @booking.end_date.strftime("%A %d %B %Y") %></p>
      <p>Pour un montant de <%= (@booking.deal.price * 4) * @demi_journées %> € / par personne</p>
      <p><%= link_to "Demander", demande_path(@booking) %></p>
    </div>
  </div>
</div>
